{
  "name": "fsm-faq-update-broker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that serves FSM FAQ plugin update metadata and zips (PUC broker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:temporary": "wrangler deploy --temporary",
    "sync:local": "node scripts/sync-release.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
